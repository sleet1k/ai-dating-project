# Текущий спринт: Подготовка к Релизу v1.0 (Сейчас: v0.9)

## Активные задачи до первого релиза (v1.0):
- [x] **Этап 1:** Перевести `vlm_analyzer.py` с LM Studio (`openai`) на `google-genai` (`gemini-2.5-flash`).
  - [x] Кэшировать `my_profile.json` при старте.
  - [x] Перенести английский Prompt-Tagging в `system_instruction`.
  - [x] Включить нативный JSON Mode (`response_mime_type="application/json"`).
  - [x] Ограничить `reason` до 10-15 слов.
- [ ] **Этап 2:** Интерактивное управление критериями в CLI (`config/settings.py`).
  - [ ] Добавить UI редактирования `criteria.txt`.
  - [ ] Реализовать функцию «✨ Улучшить вкус» (трансформация сырого текста через Gemini в чистые критерии).
- [ ] **Этап 3:** Написание основной документации (`README.md`, `ARCHITECTURE.md`, `PROMPTS.md`, `ROADMAP.md`).
- [ ] **Этап 4 (Релиз v1.0):** Сборка всего проекта в один автономный `.exe` файл.
